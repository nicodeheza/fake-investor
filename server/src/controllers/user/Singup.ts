import express from "express";
export default function Singup(req: express.Request, res: express.Response) {
	//check email format
	//check email exist
	//check user name exist
	//hash password
	//db... etc
	console.log(req.body);
}
